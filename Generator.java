public class Generator {
    

    public static void generate(Token[] tokens){
        // return signifies the end of node, so we generate the assembly for returning a value
        if(tokens[0].getTokenType() == TokenType._return){
            /**
             * Write assembly here
             */
        }

    }
    
}
